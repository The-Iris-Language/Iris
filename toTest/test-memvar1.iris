class Dog {
    public: 
        string color;

        void setColor(string c) {
            color = c; .. this should be self.color which we haven't implemented yet
        }
} 

class Main {
    public: 
        univ int main() {
            .. constructors
            Dog d = new Dog();
            ..d.setColor("brown"); .. need to implement this
            Olympus.print(d.color);
        }
}

.~* 
    1. Build SAST classes
        - may include reordering
    2. Define struct/class type in codegen
    3. Add member vars/locals to a string map -> stores registers/address of location of things
    4. Implement constructors
*~.