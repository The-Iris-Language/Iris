.. Tests declaring local object instance in main (no constructor)
class Animal () {
    public:
        int legs;
        int eyes;
        bool fur;
        
        string noise(int volume) {
            return "ruh roh raggy";
        }

    permit: 
        string color;
        bool alive;
}

class Main () {
    public:
       int univ main () {
            Animal a;
            Olympus.println("Animal declared in main");
            return 0;
       }
}