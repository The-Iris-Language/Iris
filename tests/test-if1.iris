.. Test true case of if (no else)
class Main () {
    public:
        int univ main () {
            bool b = true;
            if (b) {
                Olympus.println("true then case");
            } 
            return 0;
        }
}